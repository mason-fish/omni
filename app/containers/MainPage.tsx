import React from 'react';
import Main from '../components/Main';

// TODO: This is might not need to be a 'container', since it is currently just a pass-through
export default function MainPage() {
  return <Main />;
}
